<%= turbo_frame_tag "hotels_page_#{page}" do %>
  <% hotels.each do |hotel| %>
    <%= render 'shared/hotel_list_item', hotel: hotel, city_display_name: city_display_name %>
  <% end %>

  <% if has_more %>
    <%= turbo_frame_tag "hotels_page_#{page + 1}", src: send(path_helper, url_slug, page: page + 1), loading: :lazy do %>
      <li class="flex justify-center py-8">
        <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-gray-900"></div>
      </li>
    <% end %>
  <% end %>
<% end %>
