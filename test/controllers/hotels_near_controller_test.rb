require "test_helper"

class HotelsNearControllerTest < ActionDispatch::IntegrationTest
  test "should get show" do
    # This test requires a valid city and neighborhood slug
    # Update with actual test data when available
    skip "Need valid city and neighborhood test data"
  end
end
